{
  "name": "@quack/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "tauri": "pnpm dlx @tauri-apps/cli@2.0.0",
    "dev:stack": "pnpm --dir ../.. dev",
    "build:sidecar": "pnpm --dir ../.. build:sidecar",
    "dev": "pnpm dlx @tauri-apps/cli@2.0.0 dev",
    "build": "pnpm build:sidecar && pnpm dlx @tauri-apps/cli@2.0.0 build",
    "icons": "node scripts/generate-icons.js"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-shell": "^2.0.0"
  }
}
